// Copy this file to config.js and replace with your real Cesium Ion token.
// config.js is gitignored — never commit your actual token.
//
// Get a free token at: https://ion.cesium.com/tokens
//
// LOCAL DEV: add `localhost` to the token's Allowed URLs in the Ion dashboard,
//   or use an unrestricted token — a token restricted to the Pages domain will
//   NOT authenticate on localhost (blank globe).
// DEPLOY: the GitHub Pages workflow (.github/workflows/deploy.yml) writes this
//   file from the CESIUM_ION_TOKEN repo secret at build time — nothing to commit.
const CESIUM_ION_TOKEN = "YOUR_CESIUM_ION_TOKEN_HERE";
