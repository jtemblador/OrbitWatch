# OrbitWatch — AI Context

## What This Project Is
A satellite orbit tracker and conjunction screener. Fetches real satellite GP/OMM data, propagates orbits using a from-scratch C++ SGP4 engine, visualizes them on a Cesium.js 3D globe, and detects close approaches between satellites — a geometric screener (TCA, miss distance, relative speed, RTN) **validated against CelesTrak SOCRATES to machine precision on epoch-matched elements**. Deliberately *not* operational collision avoidance: no probability-of-collision (needs covariance we don't have). (An ML risk classifier was the original plan; dropped Jun 11 — see the Pivot note below.)

## Who This Is For
Jose Temblador — CS honors student (CSUDH, graduating May 2026) building this as a portfolio project to stand out when applying to aerospace/defense companies in the South Bay LA area (SpaceX, Northrop Grumman, Boeing, Aerospace Corp, K2 Space, Hadrian).

## Tech Stack
- **Compute core:** C++ with pybind11 (SGP4 propagation + conjunction cascade: coarse + medium + fine)
- **Coordinate transforms:** GMST Z-rotation (TEME→ECEF) + SPICE recgeo (ECEF→geodetic)
- **Conjunction validation:** CelesTrak **SOCRATES** (open, SGP4-based → same method) + Space-Track **`gp_history`** epoch-matching (`spacetrack_fetcher.py`). Orekit was evaluated and dropped — SOCRATES is the anchor.
- **Backend:** Python, FastAPI, uvicorn
- **Frontend:** Cesium.js (industry-standard 3D globe), vanilla JS
- **Data:** CelesTrak (OMM/JSON format, not legacy TLE) + Space-Track (`gp_history` for validation; `cdm_public` is the optional 8.6 stretch)
- **Storage:** Pandas, Parquet files
- **Deployment:** Static website — CI-built `snapshot.json` (active payloads ~11k, screened offline; debris deferred) on GitHub/Cloudflare Pages, client-side `satellite.js` propagation, + compressed snapshot archive (Docker dropped Jun 24; see roadmap Phase 9)

## Architecture
```
Cesium.js Frontend (3D Globe)
        ↕ REST API (JSON)
FastAPI Backend (Python)          [local dev; the deployed site is a static export]
   ├── GP/OMM Fetcher (CelesTrak / Space-Track)
   ├── C++ Core (pybind11)
   │   ├── SGP4 Propagation Engine
   │   └── Conjunction Cascade (coarse + medium + fine → TCA/miss/RTN)
   ├── Coordinate Transforms (TEME → GMST rotation → ECEF → SPICE geodetic)
   └── SOCRATES Validation (offline: fetch SOCRATES → screen → match → epoch_ok via DSE)
```

## Dataset Scaling Path
1. Space Stations (~30 objects) — Phase 1 (current)
2. Brightest/Visual (~150 objects) — Phase 2
3. Starlink (~6,000 objects) — Phase 3
4. Full catalog + debris (10,000+) — Phase 4

## Key Files
- **Project plan:** `PROJECT_PLAN.md`
- **Roadmap:** `progress/roadmap.md`
- **Scaling tracker:** `progress/scaling_tracker.md` (Phase 3 perf items)
- **Weekly plans:** `progress/week{N}_plan.md`
- **Task logs:** `progress/task_logs/task_{N}_{slug}.md`
- **Key findings:** `progress/notes/key_information.md`
- **C++ extension source:** `orbitcore/` (CMakeLists.txt, src/, include/)
- **C++ compiled module:** `backend/orbitcore.cpython-312-x86_64-linux-gnu.so` (rebuild via `orbitcore/build` → `cmake .. -Dpybind11_DIR=$(python -m pybind11 --cmakedir)` → `make` → `cp orbitcore*.so ../../backend/`). **Source layout (10.2 split, one topic per file):** `src/screening.cpp` + `include/screening.h` = the pure-C++ engine (coarse cut, 6.3 medium scan, **10.2 sieve: anchors + interval builder + activation-list scan**, **10.4 fine stage: `refine_rows` OpenMP Newton + superset ellipsoid pre-cut** — no pybind11); `src/bindings.cpp` (37-line module entry) + `bind_satrec.cpp` / `bind_propagation.cpp` / `bind_screening.cpp` (the Python boundary: validation, GIL, conversion; incl. `screen_pairs(time_filter=…, refine=…)` + the `_sieve_anchors`/`_refine_oracle` oracle-lock hooks). CMake links OpenMP optionally (absent → single-thread, same results)
- **Backend entry point:** `backend/main.py`
- **API routes:** `backend/routers/satellites.py`
- **Core pipeline:** `backend/core/propagator.py` (+ `catalog_size()` for the screening cap), `tle_fetcher.py` (fetch + `slice_to_shell` + `build_starlink_shell`; 7.4 `_derive_object_type` from the CelesTrak name — gp.php omits OBJECT_TYPE), `coordinate_transforms.py`, `conjunctions.py` (ConjunctionScreener + run_screen + fine_filter; 7.2 SFS-ellipsoid path + co-located suppression + de-dupe; **7.3 `fine_filter_batch` — batched range-rate-Newton fine stage; fine_filter kept as oracle**; **10.1a `run_screen(fused=True)` → `orbitcore.screen_pairs`, byte-identical events, survivor pairs never enter Python**), `screening_volumes.py` (7.2 — SFS Table 3 RTN ellipsoids + `regime_for`; **9.2 `is_screenable`** — True iff a genuine handbook volume exists, LEO 1-4 + GEO band), `snapshot.py` (**9.2** — pure `build_snapshot(df, events, params)` → the static-site `snapshot.json`: OMM per object for satellite.js `json2satrec` + conjunctions + meta), `demo_seed.py` (**9.9:** `build_synthetic_shell` test/profiling fixture only — the crosser was removed), `socrates_fetcher.py` (**8.1** — SOCRATES validation data: bulk-CSV fetch + `top_n`/`by_name`/`by_catnr`/`between`), `http_fetch.py` (**8.1** — shared `download_text`, requests→curl-TLS-fallback, used by both fetchers), `socrates_compare.py` (**8.2** — `match_events` pure matcher + `compare_against_socrates` orchestrator + `epoch_ok` via DSE; **8.3** + `build_epoch_targets`), `spacetrack_fetcher.py` (**8.3** — Space-Track cookie auth + `gp_history`/`gp` fetch + `_coerce_numeric` for ST's all-string JSON + `BulkGPAdapter` epoch-matched/latest via the `fetch_by_catnr` seam), `socrates_report.py` + `socrates_plots.py` (**8.3** — pure Markdown formatters + matplotlib figures); propagator now exposes `build_satrecs_and_meta(df)` (**8.2** — GP frame → screener input)
- **Validation report runner:** `scripts/validate_socrates.py` (**8.3** — `--epoch-matched` / `--current-source {celestrak,spacetrack}`; writes `validation/socrates_report.md` + figures; auto-loads a gitignored `.env` for Space-Track creds)
- **Snapshot pipeline (Phase 9 deploy):** `scripts/build_snapshot.py` (**9.2** — fetch a group → screen handbook-covered orbits → write `snapshot.json`; `--group --hours --mode {sfs,euclidean} --max-sats --out`). Ships **OMM, not TLE** (satellite.js `json2satrec`); **displays all, screens only LEO+GEO** (`is_screenable`); NaN-safe. The static site reads this one file — no per-visit API calls. **10.6:** runs the accelerated engine (`_ACCEL = fused+sieve+refine`); the CI invocation dropped `--max-sats` → screens the **full ~16k catalog**.
- **Validation docs:** `validation/socrates_report.md` (generated, the SOCRATES match results) + `validation/sgp4_uncertainty.md` (**8.4** — hand-authored cited limits/uncertainty doc; report links it via a `## Limitations` footer)
- **Frontend entry point:** `frontend/index.html`
- **Frontend JS (9.3 — static, snapshot-driven, zero `/api` per visit; 9.4 — perf + filters; 9.6 — conjunction-first UX + search):** `frontend/js/app.js` (viewer; **9.4 `requestRenderMode` + 30 fps rAF driver so the GPU idles when paused/tab-hidden — the heat fix**), `clock.js` (simulated clock + time bar), **`snapshot-data.js` (9.3 loader + satellite.js satrec cache + OMM→metadata via Kepler + `_isoToEcma` Safari-safe timestamps; 9.4 `SAT_GROUPS`/`_regime`/`_classifyGroup` — 5 mutually-exclusive display groups)**, **`propagation-worker.js` (9.3 web worker: whole-catalog SGP4 → transferable Float32Array ECEF + per-sat ok-mask)**, `satellites.js` (points + lerp + **lazy labels above 400 sats** + **9.4 `animationTick` 30 fps loop + dots colored by group + hide-group-stops-work**), `info-panel.js` (click → panel + orbit trail + nadir line, client-side; **9.4 name wraps not truncates**), `controls.js` (**9.4 Display tab = group filters w/ counts + swatches, `revealGroups`**), `conjunctions.js` (snapshot list + TCA playback + freshness line + report link; **9.4 `focusedPair`/`handleParticipantHidden`; 9.6 top-20+Show-more, group-colored names, miss-gradient, TCA ephemeris (ground marker), fading TCA arcs (clickable, batched), soft-focus + ⤓ Jump to TCA**), **`search.js` (9.6 — top-center autocomplete: name/NORAD/alias → select + fly + reveal)**. **9.6 across the stack:** "Conjunctions only" either/or views (Top-20 arcs / All participant dots) as the **startup default** (filters off, ~557 objects on load); per-sat exclusive reveal (`revealedSats`); **heat fix #2** — `requestRender` only when a visible point moved (empty globe ≈ 0 renders) + **worker participant mask** (`refreshSatellites(force)` + `pendingForcedRefresh` — see key_information); editable HH:MM:SS clock (Escape-cancel flag); group-colored **Earth-occluded** trails (far-side ghost removed). satellite.js 6.0.1 pinned CDN; **cross-validated 0.00 m vs our C++ engine**. **10.6 full-catalog perf/UX round:** display cap to the closest-N conjunctions (`CONJ_DISPLAY_CAP=500` in `snapshot-data.js`; header "closest N of total"), **focus isolation** (`isolationSet`/`isolationMask` — hides all but the involved sats AND masks the worker to them, ~2–3 propagations/frame), occlusion throttle + 15 fps (`satellites.js`), unified point styling (hover > selected/pair > plain) + hover emphasis, Escape **zoom stack** (All → Conjunction View → TCA View) + top-center **POV badge** (`conjunctions.js`), both-participant pair highlight, rewind/pause **transport** (−10/−5/−1×) + **spacebar** pause/resume (`clock.js`), TCA-time ground marker, **Labels toggle removed** (auto-labels the isolated handful, `_wantsLabel`), duplicate-orbit-trail fix
- **Deploy (Phase 9.4 deploy + 9.5 robot job — LIVE):** `.github/workflows/deploy.yml` — GitHub Pages deploy; **reuse-vs-rebuild** (frontend push reuses the live `snapshot.json` ~1 min; `workflow_dispatch rebuild_snapshot=true` re-fetches+screens ~8 min); builds the `.so`, screens `active --max-sats 5000 --hours 24`, injects the domain-restricted Ion token from the `CESIUM_ION_TOKEN` secret. **9.5 robot job:** `schedule:` cron (05:17/13:17/21:17 UTC, trailing SOCRATES's 3×/day) forces REBUILD to self-refresh the site + archives each snapshot to an **orphan `data` branch** (`snapshots/<ts>.json.gz`, append-only, in a `$RUNNER_TEMP` worktree, **`continue-on-error`** so it never blocks the site refresh); least-privilege per-job perms (top-level `contents: read`, `build` overrides to `write`). **10.6:** rebuild fetches once → **two-tier A/B gate** (`ab_screen.py`, `set -e` fails build → no deploy: Tier 1 classic==flip @5k, Tier 2 sieve-off vs on @full/6h, memory-safe) → screens the full catalog; the gate is **SKIPPED on scheduled cron** (guards code not data → cron ~10 min, manual/push runs the full ~50-min gate). **Live: https://jtemblador.github.io/OrbitWatch/ (full ~16k catalog)**
- **Pydantic schemas:** `backend/models/schemas.py` (10 response models)
- **Profiling harness:** `scripts/profile_screening.py` (7.1 — sweeps the cascade over the Starlink catalog; `run_screen(timings=…)` is the passive hook; **10.0** `--source active --mode sfs --start` profiles the exact CI operating point — head-slice + screenable filter, matching `build_snapshot.py`; **10.2** `--fused --sieve`)
- **Stage-1 A/B harness:** `scripts/ab_screen.py` (**10.3** — load one catalog, screen twice (baseline vs fused+sieve[+refine]), diff the event lists; **exit 0 iff byte-identical** so CI can gate on it — 10.6's two-tier in-CI gate runs this. `--baseline classic` at ≤5k; `--baseline fused --refine` at full scale — **10.6:** the fused baseline honors `--refine` too so both sides keep the C++ fine pre-cut and stay memory-safe (3.24 GB, not the ~13 GB OOM))
- **Phase-10 gate prototypes/oracles:** `progress/week10_planning/path_gate.py` (10.0 — path bound + ceiling + no-skip checker) + `gate_sanity.py` (6 geometry cases) + **`time_filter_gate.py` (10.1b — the validated time-filter construction: 3-point anchor → rv2coe → equinoctial chord rates → per-sat curvature margins → TCA-coverage check; the executable spec + 10.2/10.5 oracle)** + **`stage1b_timefilter_spec.md` (the prose spec + results table)**
- **Tests:** `tests/` (581 tests across 15 test files; 10.1a +7 `TestFusedStage`, 10.2 +5 `TestTimeSieve`, 10.4 +8 `TestCppFine`, 10.5 +5 `TestFlipContract`)

## Related Projects & Files
- **Resume:** `/home/j0e/Portfolio/JoseTrinidadTemblador_Resume.pdf`
- **NFL ML Project (similar pipeline pattern):** `/home/j0e/Projects/Sports Analyzer/`
- **Job Tracker:** `/home/j0e/Projects/Job Tracker/`

## Current Status
- **Phase:** **PHASE 10 COMPLETE (Jul 15) — the project is LIVE screening the full ~16k active catalog** (https://jtemblador.github.io/OrbitWatch/), gated byte-identical in CI. History: Phase 6 COMPLETE (6.0–6.10); **Phase 7 COMPLETE (7.0–7.5)**: 7.0 (live data) + 7.1 (scale profile) + 7.2 (SFS ellipsoid volumes) + 7.3 (fine-stage batching + cap/threadpool) + 7.4 (type filters — object_type derived from CelesTrak name) + 7.5 (scale/perf regression tests). **Phase 8 done** (8.1–8.5; 8.6 `cdm_public` = deferred stretch). **Phase 9 underway (deploy as static site): 9.1 (clean stale refs) + 9.2 (snapshot pipeline) + 9.3 (frontend reads `snapshot.json` + web-worker satellite.js propagation, zero `/api` per visit) + 9.4 (deployed to GitHub Pages — LIVE at https://jtemblador.github.io/OrbitWatch/ + display rework: render-loop heat fix + group filters) + 9.5 (robot job: `schedule:` cron 3×/day self-refreshes the site + archives each snapshot to an orphan `data` branch) + 9.6 (live-QA → UI round: search bar, conjunction-first views + startup default, miss-gradient/red-TCA/ephemeris, editable clock, heat fix #2 + worker mask — 2 review rounds, 4 findings fixed) + 9.7 (README rewrite — live URL + screenshots + current-vs-epoch-matched validation table) + 9.8 (demo GIF — 89 s live-site walkthrough → `docs/img/demo.gif`, README hero) + 9.9 (final cleanup — demo seed removed, census tests made catalog-relative, 556 passing 3× clean) done — **Phase 9 COMPLETE; 9.10 (prediction-evolution view) = optional stretch.** Interactive conjunction-UX core already done (early `task_9_1_conjunction_ux.md`). **Phase 10 (post-launch, COMPLETE): 10.0 measurement gate done (Jul 8) — the planned path filter measured out (0.002–1.9% cut; co-altitude circular orbits intersect at their mutual nodes, nothing geometric to drop) and the phase was RE-SCOPED: Stage 1 = fused C++ time-sieve (98–99% of medium pair-step work removable, kills the 48 M-pair/8.7 GB Python materialization), Stage 2 = C++ fine stage (OpenMP + streamed — fine is 47–56% of the cascade and the actual cap blocker; full ~16k screen extrapolates to ~25 GB, won't fit the 16 GB CI runner). Both stages gate on byte-identical events; no-skip contract is EVENT-level (measured: medium over-flags by design). See `task_logs/task_10_0_measurement_gate.md` + rewritten roadmap Phase 10. 10.1a done: `screen_pairs` fused C++ coarse+medium (survivor pairs never enter Python) — byte-identical events, +6 tests (+1 review: partial-cut lock, 563); measured correction: the Python pair list is only ~0.35 GB (CI slice) / ~5.4 GB (full 16k) of the ~25 GB, so the fine-stage dicts (#7 / Stage 2) are the bigger memory term — 1a alone doesn't fit the full catalog. See `task_logs/task_10_1a_fused_stage.md`. 10.1b done (GO): the time filter validated no-skip in the prototype BEFORE any C++ — 1,403,999 real events across 4 catalogs checked at their TCAs, 0 uncovered @ 0.25° margin (ship 0.5°), mutation bites everywhere, 5 km SOCRATES-mode holds; realized medium-scan cut ~41× (CI slice) / ~130× (Starlink) / ~168× (full active), pinned to the near-coplanar floor. Four measured failure modes cured (TCA-not-flag contract; anchor-at-start not epoch — 35° drag divergence at 21.8 d; equinoctial λ chord — osculating (ω,M) ill-conditioned below e≈0.002; per-sat measured curvature margins — reentering sats need ~6°). Oracle+spec in `week10_planning/`; Satrec gained read-only mdot/argpdot/nodedot. See `task_logs/task_10_1b_timefilter_gate.md`. **10.2 done: the sieve is IN the C++ engine** (`screen_pairs(time_filter=True)` / `run_screen(sieve=True)`, default OFF until 10.3+10.6 flip production) — C++ anchors ≡ oracle to 2e-11 (test-locked); **events byte-identical on/off on every catalog (253,392==253,392 full Starlink); medium 4.4–6.2× faster; CI-point cascade 168→~80 s (~2×, fine now dominates → 10.4)**; `run_screen` event sort now a total order (tie-order fix); review bounded the node-path nonlinearity with the midpoint chord deviation. +5 tests (`TestTimeSieve`); 568 passing. See `task_logs/task_10_2_time_sieve.md`. **10.3 done (Jul 9): formal Stage-1 validation on catalogs the sieve never saw** — caches refreshed to median epoch age 0.54 d via a Space-Track bulk-`gp` fallback (CelesTrak TLS-blocked; active population preserved, Starlink +230 new sats incl. a week-old orbit-raising batch); **events byte-identical at every scale (312 / 312,042 / 4,445)**, suite green on fresh caches, node-aliasing residual risk 0-for-everything; same-day rows: Stage-1 **2.11×/2.22× total + RSS −23%/−26%** (5k/10k) vs classic baselines that reproduce 10.0; **post-sieve fine = 73–81% of the cascade → 10.4**; ships `scripts/ab_screen.py` (the 10.6 CI A/B harness, exit-code-gateable). See `task_logs/task_10_3_stage1_validation.md`. **10.4 done (Jul 9): the C++ fine stage (Stage 2)** — `screen_pairs(refine=True)` / `run_screen(refine=True)` (default off until 10.6): OpenMP Newton refinement with a **superset pre-cut** (per-pair ε-inflated RTN ellipsoid; C++ decides only which rows might pass, Python's `fine_filter_batch` re-refines the 0.12–0.36% survivors → **events byte-identical by construction**; ε measured: |ΔTCA|≡0, |Δmiss|≤5.7e-14 km on 934k rows). Identity on every catalog incl. classic-vs-full-flip; thread-count invariant. **CI point 146.8→32.1 s / RSS 0.50 GB; 10k 874.9→118.1 s / 1.89 GB; full 15,709/24 h: ~25 GB-impossible → 304.6 s / 5.38 GB** (closes `#7` by construction; medium now ~88% of remaining full-catalog cost). +8 tests (`TestCppFine`); 576 passing 3× clean. See `task_logs/task_10_4_cpp_fine_stage.md`. **10.5 done (Jul 9): mutation-checked property tests for the flip** — `TestFlipContract` (5) + a hostile composite fixture (3 shells + drag/eccentric singletons, verified to participate) locks `classic == fused+sieve+refine` offline (1,826 events) with **both stages asserted engaged**, the SFS ellipsoid path, flip determinism, sieve-margin wiring, and a committed refine-pre-cut mutation bite; every lock **proven to bite** via compiled mutations (revert after). Honest limit: the sieve event-level zero-margin bite is unconstructible on synthetic geometry → left to 10.6's in-CI A/B. Two review rounds. +5 tests; **581 passing 3× clean**. See `task_logs/task_10_5_property_tests.md`.**
- **Timeline:** Mar 20 – Jul 10, 2026 (Weeks 0–5 ✅; Final Sprint = Phases 6–9, Jun 12 – Jul 10)
- **Completed:** Weeks 0–5 (setup, C++ SGP4 engine, coordinate transforms, GP fetcher, propagator wrapper, FastAPI backend with 6 endpoints, Pydantic response models, 82 API tests, Cesium.js globe, satellite points with interpolation, info panel with click interaction, orbit trail at orbital altitude via TEME API + GMST rotation, selection indicator, nadir line with real-time tracking, display controls panel with label toggle, simulated clock with play/pause/speed + adaptive refresh + TEME trail re-rotation)
- **Next steps:** **Phase 10 COMPLETE (Jul 15) — the live site screens the full ~16k active catalog, gated byte-identical in CI.** No required work remains. Optional/backlog: **9.10 (stretch)** prediction-evolution view from the `data`-branch archive; per-sat propagation refcount in the sieved scan (medium is ~88% of the remaining full-catalog cost — only matters if the cron's ~10-min rebuild ever needs trimming); debris/R-B secondary screening (backlog); Space-Track `cdm_public` cross-method check (8.6 stretch). Everything else through 10.6 is done and live. **10.5 done** (`task_logs/task_10_5_property_tests.md`): `TestFlipContract` (5) + hostile composite fixture locks `classic == fused+sieve+refine` offline (1,826 events, both stages asserted engaged) + SFS path + determinism + margin wiring + a refine-pre-cut mutation bite; every lock proven to bite via compiled mutations; sieve event-level zero-margin bite left to 10.6's in-CI A/B (unconstructible on synthetic geometry); 581 passing 3× clean. **10.6 done (Jul 15) — PHASE 10 COMPLETE, LIVE on the full catalog:** `build_snapshot.py` flipped to `fused+sieve+refine`; `deploy.yml` fetches once → **two-tier A/B gate** (`set -e` fails build → no deploy) → screens the **FULL ~16k catalog** (cap removed). **Gated byte-identical IN CI on live data (Tier-1 308==308, Tier-2 3,887==3,887)**; live site verified. `ab_screen.py`'s fused baseline honors `--refine` so Tier-2 is memory-safe at full scale (3.24 GB). Cron rebuilds SKIP the ~40-min gate (guards code, not data) → 3×/day stays ~10 min. Plus a **frontend perf/UX round**: display cap to the closest 500 conjunctions (screen stays complete; header "closest N of total"), focus isolation (worker masked to the involved 2–3 sats), occlusion throttle + 15 fps; Escape zoom stack (All → Conjunction View → TCA View) + POV badge, pair highlight, hover, rewind/pause transport + spacebar, TCA-time marker, Labels toggle removed, duplicate-trail fix. `scaling_tracker #3/#7/#8` resolved. See `task_logs/task_10_6_ci_flip_cap_and_ux.md`. **10.3 done** (`task_logs/task_10_3_stage1_validation.md`): formal Stage-1 pass on never-seen catalogs (Space-Track bulk-`gp` refresh, median epoch age 0.54 d, +230 new Starlinks) — **312 / 312,042 / 4,445 events byte-identical** (CI point / full Starlink / full active), classic re-baselines reproduce 10.0 (146.8 s/1.72 GB; 874.9 s/8.69 GB), Stage-1 2.11×/2.22× + RSS −23%/−26%, ships `scripts/ab_screen.py`; measurement batch 55m 37s, per-step times in the log. **10.1a done** (`task_logs/task_10_1a_fused_stage.md`): `orbitcore.screen_pairs` fuses coarse+medium in one GIL-released C++ call (shared `run_medium_scan` helper; `medium_filter` refactored, its 143 tests byte-identical) → `run_screen(fused=True)`, **byte-identical events** (Euclidean shell + real active slice 78 SFS events). **Measured correction to the plan:** the Python pair list is ~0.35 GB (CI slice) / ~5.4 GB (full 16k) — NOT the bulk of peak RSS; the fine-stage dicts (`#7`) are bigger, so 1a alone doesn't fit the full catalog (needs 10.4). CI-point RSS 1.71→1.34 GB. +6 tests; 562 passing. **10.0 done** (`task_logs/task_10_0_measurement_gate.md`): one-day NumPy gate on real catalogs BEFORE any C++ — path filter (realistic margins) drops **0.002% / 1.9% / 0.4%** (Starlink / CI-slice / full active) → rejected; **time-filter ceiling 0.31–1.86%** of medium pair-step work (~54–320×) → the sieve to build; **0 event-level no-skip violations** everywhere (flag-level is the wrong contract — medium over-flags by design); production profiles: CI point 157 s (medium 81 / fine 74, 1.7 GB), 10k 852 s (368/479, 8.7 GB), full 15,708 → ~25 GB extrapolated (won't fit CI — memory, not time, is the cap). Prototype/oracle preserved in `progress/week10_planning/`; profiler gained `--source active --mode sfs --start`. **9.9 done** (`task_logs/task_9_9_final_cleanup.md`): demo seed removed (`ORBITWATCH_DEMO_SEED` + `append_demo_crosser` + `seed_demo` + 7 feature tests; `build_synthetic_shell` kept for the scale tests/profiler; `TestDenseShellScale` reworked onto natural plane-crossings). Incidental: two Week-2 tests asserted an absolute census (≥25) of the live-churning stations group (23 now) — made relative to `catalog_size()`; **556 passing 3× clean**. **9.8 done** (`task_logs/task_9_8_demo_gif.md`): 89 s live-site walkthrough → `docs/img/demo.gif` (chrome-cropped, two-pass palette, 9 MB) = the README hero. **9.7 done** (`task_logs/task_9_7_readme.md`): README rewritten (~95 lines) — live URL + 2 live-site screenshots (`docs/img/`), no-Pc framing, robot-job→snapshot→globe diagram, **current-vs-epoch-matched validation table** (lesson: a table of perfect 0.000s reads as placeholder — show the controlled contrast; user caught it), verified run-locally commands. Last stale ML framing gone. **9.6 done** (`task_logs/task_9_6_live_qa_ui_round.md`): live-site QA became a user-driven UI round — **search bar** (name/NORAD/alias), **conjunction-first UX** (top-20 + Show more, group-colored names, red TCA, miss-distance gradient, TCA ephemeris w/ ground lat/lon, clickable fading TCA arcs, ⤓ Jump to TCA, per-sat exclusive reveal, **startup = "All conjunctions" view w/ filters off**), **editable HH:MM:SS clock**, group-colored Earth-occluded trails, **heat fix #2** (`requestRender` only on visible movement — empty-globe Chrome fan fix) + **worker participant mask** (4,999→557 propagations/batch). **2 adversarial review rounds → 4 findings fixed + repro-verified** (paused-mask staleness, Escape-cancel, mode-switch teardown, `workerBusy` race); Playwright-MCP-verified, zero console errors. **9.5 done** (`task_logs/task_9_5_robot_job_archive.md`): `deploy.yml` `schedule:` cron (05:17/13:17/21:17 UTC, trailing SOCRATES's 3×/day) forces the REBUILD path so the site self-refreshes, and every rebuild **archives the snapshot to an orphan `data` branch** (append-only `snapshots/<ts>.json.gz`, in a `$RUNNER_TEMP` worktree, **`continue-on-error`** so the archive never blocks the site refresh); least-privilege per-job perms (top-level `contents: read`, `build` → `write`). **2 review rounds / 6 findings fixed** incl. a *reproduced* 2nd-run crash (base the worktree on `FETCH_HEAD`, not `origin/data` — checkout@v4's narrow refspec never creates the tracking ref) + rc-based `ls-remote` detection (0/2/error). Cron **activates once on `main`** (schedule fires from the default branch); ⚠ GitHub auto-disables it after 60 days no-commits (silent). Closes `scaling_tracker #2` for prod. **9.4 done — the site is LIVE** (`task_logs/task_9_4_deploy_github_pages.md` + `task_9_4_display_rework.md`): GitHub Pages deploy via `deploy.yml` (build `.so` → screen `active --max-sats 5000 --hours 24` → inject Ion token from secret → publish; **reuse-vs-rebuild** so a frontend push redeploys in ~1 min without re-screening), **zero backend/CelesTrak calls per visit verified**; live run 5000 sats / 367 conj / 6.3 min / 0.32 MB gz. **Full ~16k screen was 20–38 min on the shared runner → capped at 5000 (Phase-10 path filter is the lever).** Plus the **display rework** (prompted by the live site running hot): **`requestRenderMode` + 30 fps rAF loop idles the GPU when paused/tab-hidden** (the heat fix), **5 group filters** (Starlink/Stations/Navigation-MEO/Other-LEO/GEO) with colored dots + counts that stop per-frame work when hidden, name-wrap; **2 review rounds, 6 findings all fixed + headless-verified**. **9.3 done** (`task_logs/task_9_3_frontend_snapshot.md`): the frontend is now **static + snapshot-driven** — `snapshot-data.js` loads `snapshot.json`, `propagation-worker.js` propagates the whole catalog in a **web worker** (`satellite.js@6.0.1` `json2satrec`, transferable Float32Array ECEF), the old lerp kept, **all `/api/*` deleted** (verified zero backend calls). **Lazy labels above 400 sats** (11k points OK in 1 draw call, `LabelCollection` isn't). **11k perf: 36 fps steady on Intel UHD 620, ~25 ms/batch, animation overhead unmeasurable — object-budget cut unused.** Cross-val: satellite.js ≡ our C++ SGP4 **0.00 m**, `computePositionGd` ≡ C+++SPICE ~2 m. **Safari fix (review catch):** `_isoToEcma` truncates the snapshot's 6-digit-µs timestamps to strict-3-digit ECMA (WebKit `Date`/`json2satrec` reject non-3-digit fractions → silent NaN epochs; V8 is lenient so it's invisible in local dev). +"updated X ago" freshness line + validation-report link. **65-check Node harness on the real sources** (mutation-verified, 3× clean). **9.2 done** (`task_logs/task_9_2_snapshot_pipeline.md`): `backend/core/snapshot.py` + `scripts/build_snapshot.py` produce the static-site `snapshot.json` = `meta` + `satellites[]` (**OMM** for satellite.js `json2satrec` — cross-validated **0.105 m** vs our C++ engine) + `conjunctions[]`. **Displays all ~11k, screens only handbook-covered orbits** (`is_screenable` — LEO 1-4 + GEO band; MEO/HEO shown, not screened). NaN-safe (`allow_nan=False`); ~0.7 MB gz at 11k. **9.1 done** (`task_logs/task_9_1_cleanup_stale_refs.md`): cleaned stale ML/Orekit/Docker refs from PROJECT_PLAN/CLAUDE/requirements/main.py/memory; `week0_*` banner-flagged; journal + README (→9.7) left. **8.4 done** (`task_logs/task_8_4_sgp4_uncertainty.md`): `validation/sgp4_uncertainty.md` (linked from the report's `## Limitations` footer) — cited, evidence-backed limits doc separating implementation error (**<1 m** vs Vallado ref) from element accuracy (~1 km), age growth (~2–3 km/day), and **epoch drift** (the dominant term, *measured* in 8.3: current-feed Δmiss 1.1–2.6 km → **0.000** matched). Frames it as honest screening, not collision avoidance (no Pc). Citations verified (MathWorks Finder, NASA/SP-20205011318, Vallado AIAA 2006-6753 for *implementation* fidelity, Skyfield/CelesTrak). **8.3 done** (`task_logs/task_8_3_validation_report.md`): `scripts/validate_socrates.py` → `validation/socrates_report.md` + figures, 3 slices. **Stage A** current-GP baseline; **Stage B** = the `gp_history` lever (`spacetrack_fetcher.py` — Space-Track cookie auth/no-key, `_coerce_numeric` for ST's all-string JSON, `BulkGPAdapter` reuses the 8.2 `fetch_by_catnr` seam). **Live current→epoch-matched:** ISS 3/9→8/9, top-25 8/25→**25/25 (100%)**, Starlink-40 8/40→**40/40 (100%)**, every matched event **ΔTCA/Δmiss = 0.000** (byte-level same-method agreement); current-feed lowness is pure epoch drift (`epoch_ok=0`). Creds in gitignored `.env`; `--current-source spacetrack` bypasses VPN-blocked CelesTrak. +68 tests. **8.2 done** (`task_logs/task_8_2_socrates_compare.md`): `socrates_compare.py` — `match_events` (pure matcher: pair + TCA proximity → per-event TCA/miss deltas + reproduction rate bucketed by `DSE`; reused by Phase 9's badge) + `compare_against_socrates` (orchestrator: GP-per-object fetch with `max_objects` cap → `build_satrecs_and_meta` → 5 km Euclidean screen → match → `epoch_ok` via `DSE`). Live ISS: 3/9 reproduced, matched agree **TCA <5 s / miss <0.32 km**; `epoch_ok=0` (current `gp.php` rolled the epochs SOCRATES used) → misses cluster at higher `DSE` (the epoch-drift finding; `gp_history` backstop is the 8.3 lever). **8.1 done** (`task_logs/task_8_1_socrates_fetcher.md`): `SOCRATESFetcher` downloads the bulk **`sort-minRange.csv`** full run (~148 k conjunctions, RFC-4180 — the HTML-scrape plan was obsoleted by finding the official static CSV), parses to a typed frame (objects + both **`DSE`** + TCA + range + speed; drops `MAX_PROB`/`DILUTION`), Parquet-cached 8 h TTL; slices `top_n`/`by_name`/`by_catnr`/`between`. Shared `http_fetch.download_text` (requests→curl fallback) now used by both fetchers. Gotchas: trailing `[status]` name tag, **object 1/2 positional (not primary)**, `.str.contains` needs `regex=False`. Live: 147,814 rows, 0 nulls, closest 0.016 km. **7.5 done** (`task_logs/task_7_5_tests.md`): `TestScaleRegression` — 3 cross-task locks (deterministic + offline). (1) the 7.3 batched Newton fine stage reproduces the scipy oracle across **257 real windows** of a 300-sat shell (<10 m / <50 ms / <1 mm/s) — the "byte-identical on dense catalogs" claim, previously only on ~8 crosser windows; (2) a full shell screen is **byte-identical run-to-run**; (3) the 7.2 **no-skip wiring** (a spy confirms `run_screen(volumes=…)` passes `medium_filter` the largest semi-axis 51 km, not the radial axis or box corner). **Mutation-checked** so the green tests provably bite; 3× flake-clean. Durable gotcha → key_information: solver-vs-solver TCA equivalence must gate on a real crossing (`rel_speed>1`); a synthetic shell needs ~300 sats + 100 km pad for natural windows. **7.4 done** (`task_logs/task_7_4_object_types.md`): gp.php omits `OBJECT_TYPE`, so `object_type` was `None`→`"UNKNOWN"` everywhere and the (already-wired) `controls.js` type filters never rendered. Now derived from the CelesTrak name convention (`DEB`/`R/B` tokens, else PAYLOAD; token-matched) in `tle_fetcher`, filling only nulls across all read paths incl. `load_cached` (existing caches get types, no re-fetch). Default `stations` → PAYLOAD + DEBRIS; conjunction events read real types. SATCAT join = authoritative upgrade (deferred). **7.3 done** (`task_logs/task_7_3_fine_stage_batch.md`): the fine stage (82–87% of wall time per 7.1) is now `fine_filter_batch` — **Newton on the relative range-rate** (`t ← t − (Δr·Δv)/|Δv|²`, the operational TCA solve), all windows stepped together via `propagate_batch`, vectorized + chunked. **~3.7× faster fine stage** (same-machine A/B) with **byte-identical event counts** (124,810 full catalog); cross-validated vs the scipy oracle AND a brute-force grid. `/api/conjunctions` runs in `run_in_threadpool` under `_propagator_lock` (guards screen + position/track vs a Satrec-cache race — caught by adversarial review) + a `413` cap (`ORBITWATCH_MAX_SCREEN_SATS`, default 1500). Pure-Python, no `.so` rebuild. **Deferred:** #3 C++ coarse→medium memory fusion (cap removes its urgency), radial coarse-pad tightening, #7 fine-stage dict streaming (memory); full catalog stays batch-only (117 s / 5.2 GB). **demo operating point `MAX_SATS=300` ≈ 1.7 s @ 24 h** (was 2.6 s). **7.2** (`task_logs/task_7_2_screening_volumes.md`): SFS RTN ellipsoid volumes + co-located suppression + de-dupe — real Starlink (300) → 7 conjunctions; live stations → 0 events / 46 suppressed. ⚠ **The SFS default excludes the synthetic crosser** (radial-dominated), so a populated demo needs a **real Starlink shell**, not `stations` + seed.
- **Tests:** 581 passing + 4 skipped (opt-in live fetches) across 15 test files (10.5: +5 `TestFlipContract` — the flip contract on a hostile composite; 10.4: +8 `TestCppFine`; 10.2: +5 `TestTimeSieve` incl. the C++-anchors-vs-oracle lock; 10.1a: +7 `TestFusedStage`; 9.9: −7 with the removed demo seed; two Week-2 census asserts made catalog-relative) — suite runs offline/deterministic. Frontend JS has no pytest tests (convention); 9.3 added a **65-check Node harness** on the real frontend sources (`test_9_3_data_layer.js`, session scratch — candidate for a 9.5 CI contract check)
- **Dataset / demo modes:** default `stations` (tests stay ISS-based, cached). Env-selectable: `ORBITWATCH_GROUP=<group>` picks the catalog; `ORBITWATCH_LIVE=1` fetches fresh on load (7.0); `ORBITWATCH_MAX_SATS=N` slices a dense shell in-app. **Live dense demo:** `ORBITWATCH_LIVE=1 ORBITWATCH_GROUP=starlink ORBITWATCH_MAX_SATS=300 python backend/main.py`. Offline fallback: `ORBITWATCH_GROUP=starlink_shell` (static snapshot, rebuild via `python -m backend.core.tle_fetcher starlink-shell [path.json]`). *(The `ORBITWATCH_DEMO_SEED` synthetic crosser was removed in 9.9 — the real active catalog drives the deployed demo; prod auto-refresh = the 9.5 cron.)*
- **Conjunction-screening invariant (6.3):** the medium filter uses a velocity-aware interval bound — a crossing pair with an 8 km true miss samples at ~520 km on a 60 s grid, so plain distance thresholding misses real conjunctions. Any port/replacement must preserve an equivalent no-skip bound (see key_information.md)
- **Perf note (6.1, measured):** Python-loop-over-C++ sgp4 overhead is only ~5% — the conjunction medium filter (6.3) must keep its whole loop inside C++; `orbitcore.propagate_batch` provides batch semantics + per-sat error sentinels
- **Pivot (finalized Jun 11):** Dropped ML risk classifier (full CDM data requires an SSA Sharing Agreement — operators only; without it ML can't beat a threshold). Headline is now **conjunction screening validated against CelesTrak SOCRATES**. Scope narrowed further: **no Pc computation** (needs covariance we don't have) — this is an honest *geometric screener on SGP4*, not operational collision avoidance. Confirmed direction against Jose's targeted aerospace roles (all want C++/GNC/test-rigor, none want ML). See roadmap.

## Notes for Future Sessions
- Jose's ML experience is with CatBoost/XGBoost/LightGBM from his NFL prediction project — applies if ML is reintroduced with proper data access
- He's now comfortable with FastAPI, C++/pybind11, and SPICE — first time was this project. Still new to Cesium.js (Week 4)
- The project should be demoable and portfolio-ready by Jul 10
- Keep the code modular and well-structured — this will be shown to employers
- C++ and SPICE were chosen specifically to appeal to aerospace employers (SpaceX, K2 Space, Aerospace Corp, etc.)
- **No Pc computation** — deliberately out of scope (needs covariance data we don't have). Output is geometric: TCA, miss distance, relative speed, RTN components. Honest "screening, not collision avoidance" framing.
- Validation: CelesTrak SOCRATES is the primary anchor (open/no-auth, SGP4-based → same-method). Space-Track `cdm_public` is an optional Phase 8 stretch (SP-based cross-method check, detection-only). Orekit dropped.
- Stale ML/Orekit/Docker references cleaned from the current-facing docs + deps + memory in **9.1** (PROJECT_PLAN.md rewritten, `xgboost` dropped from requirements, `main.py` CORS comment, `week0_*` banner-flagged, `1plan.md`/`2build.md` memory fixed). The historical journal (task logs, week plans/notes, `critical_questions.md`) is intentionally left as-is; `README.md` is rewritten later in 9.7.
- Data is fetched as OMM/JSON from CelesTrak (not legacy TLE format) — future-proofs against the NORAD 5-digit catalog number cap (~July 2026)
- SPICE does NOT know the TEME frame — we handle TEME→ECEF via GMST Z-rotation, then SPICE for geodetic only
- Phase 3 scaling items tracked in `progress/scaling_tracker.md` (C++ batch SGP4, background refresh, etc.)
